<h2>Add a new comment</h2>

<%= form_for [@concert, @comment] do |f| %>
	<%= f.label :author %>
	<%= f.text_field :author %>
	<br>
	<%= f.label :content %>
	<%= f.text_field :content %>
	<br>
	<%= f.button "Submit" %>
<% end %>